function fazerAlgo(test){
    if (test) {
        
    } else {
        let _check : never // caso retorne o never, o valor da variavel nunca vai poder ser reatribuido novamente
        _check = ''
        let texto = _check
        texto = 'aa'

        return _check

        
        
    }
}