function fazerAlgo(test) {
    if (test) {
    }
    else {
        var _check = void 0; // caso retorne o never, o valor da variavel nunca vai poder ser reatribuido novamente
        _check = '';
        var texto = _check;
        texto = 'aa';
        return _check;
    }
}
